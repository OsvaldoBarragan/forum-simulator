import Link from 'next/link';

export default function Footer() {
    return (
        <footer className="relative flex flex-row justify-between items-center p-8 m-8 rounded-xl min-h-16 min-w-full w-full bg-gradient-to-t from-blue-800">
            <h1>Still in progress.</h1>
        </footer>
    );
}